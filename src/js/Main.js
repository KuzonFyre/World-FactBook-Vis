class Main {
    constructor() {
        // Initialize properties here if needed
    }

    init() {
        new Map();
    }
}

document.addEventListener('DOMContentLoaded', function () {
    const main = new Main();
    main.init();
});